import React from 'react';

const RegisterPage = () => {
    return (
        <div>
            <h1>Register Page</h1>
            <p>Welcome to the home page</p>
            <p>This is a sample app to demonstrate how to use React Router.</p>
            <p>The app is built using the create-react-app template.</p>
            <p>The app is deployed to a serverless AWS Lambda function.</p>
            <p>The app is deployed to a serverless AWS API Gateway REST API.</p>
            <p>The app is deployed to a serverless AWS DynamoDB table.</p>
            <p>The app is deployed to a serverless AWS CloudFront distribution.</p>
            <p>The app is deployed to a serverless AWS ECS cluster.</p>
            <p>The app is deployed to a serverless AWS EC2 instance.</p>
            <p>The app is deployed to a serverless AWS RDS database.</p>
            <p>The app is deployed to a serverless AWS S3 bucket.</p>
            <p>The app is deployed to a serverless AWS CodeCommit repository.</p>
        </div>
    );}
export default RegisterPage;